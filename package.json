{
  "name": "@midas/esp32-devops-mcp",
  "version": "1.3.0",
  "description": "MCP server for ESP32 development with build automation, serial port management, performance testing, and physical hardware interrogation",
  "main": "dist/index.js",
  "type": "module",
  "bin": {
    "esp32-devops-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "build:watch": "tsc --watch",
    "build:tests": "tsc -p tsconfig.test.json",
    "test": "npm run build:tests && node --test \".test-build/tests/*.test.js\"",
    "prepare": "npm run build"
  },
  "keywords": [
    "mcp",
    "esp32",
    "arduino",
    "platformio",
    "embedded",
    "iot",
    "claude",
    "devops"
  ],
  "author": "JosephR26 <josephreilly19@outlook.com>",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
